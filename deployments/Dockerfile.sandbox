FROM golang:1.24-alpine AS build
WORKDIR /src
COPY go.mod go.sum* ./
RUN go mod download
COPY . .
RUN go build -o /out/examora-sandbox ./cmd/sandbox

FROM alpine:3.21
COPY --from=build /out/examora-sandbox /usr/local/bin/examora-sandbox
EXPOSE 8081
CMD ["examora-sandbox"]
