FROM golang:1.24-alpine AS build
WORKDIR /src
COPY go.mod go.sum* ./
RUN go mod download
COPY . .
RUN go build -o /out/examora-api ./cmd/api

FROM alpine:3.21
COPY --from=build /out/examora-api /usr/local/bin/examora-api
EXPOSE 8080
CMD ["examora-api"]
